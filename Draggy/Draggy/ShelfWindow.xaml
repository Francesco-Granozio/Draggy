<Window
    x:Class="Draggy.ShelfWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Draggy"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="400"
    Height="300"
    AllowsTransparency="True"
    Background="Transparent"
    ShowInTaskbar="True"
    Title="Draggy Shelf"
    Topmost="True"
    WindowStyle="None"
    mc:Ignorable="d">
    <Border
        Padding="12"
        Background="#F0FFFFFF"
        BorderBrush="#CCCCCC"
        BorderThickness="1"
        CornerRadius="8">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="📁 Draggy Shelf" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                <Button Content="🗑️ Clear" Command="{Binding ClearAllCommand}" Margin="10,0,0,0" Padding="5,2"/>
            </StackPanel>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ItemsControlFiles" ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="4"
                        Padding="4"
                        Background="WhiteSmoke"
                        CornerRadius="4">
                        <StackPanel Orientation="Horizontal">
                            <Image
                                Width="48"
                                Height="48"
                                Margin="0,0,8,0"
                                Source="{Binding Thumbnail}" />
                            <TextBlock VerticalAlignment="Center" Text="{Binding FileName}" />
                        </StackPanel>
                        <Border.InputBindings>
                            <MouseBinding
                                Command="{Binding DataContext.StartDragCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Gesture="LeftClick" />
                        </Border.InputBindings>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
